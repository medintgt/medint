import LayoutAdmin from "@components/LayoutAdmin";

const Orders = () => {
    return (
        <LayoutAdmin>

        <div className="container mx-auto grid justify-center">
            WORKING ON CASES PAGE
        </div>
        </LayoutAdmin>
        );
};

export default Orders;